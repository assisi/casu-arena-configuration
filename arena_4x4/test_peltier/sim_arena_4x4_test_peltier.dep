# Deployment specification f;ile
# For each casu in the arena, we need to specifiy:
#     - the hostname of the target machine
#     - the controller and additional files to deploy

beearena : 
    casu-013 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-014 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-015 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-016 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-021 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-022 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-023 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-024 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-030 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-031 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-032 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-033 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-039 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-040 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
    casu-041 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_cold_hot.py
    casu-042 : 
        hostname : localhost
        user : assisi
        prefix : deploy
        controller: controllers/peltier_switching_hot_cold.py
